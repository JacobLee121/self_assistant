# Agent 维护规则

## 🔴 强制规则

**每次代码更新后，必须检查 README.md 中的架构描述是否与当前代码一致。**

具体检查项：

| 检查项 | 对应 README 章节 |
|--------|------------------|
| 项目结构是否变化（新增/删除/重命名文件） | 📁 项目结构 |
| 架构图是否需要更新（数据流、组件关系变化） | 🏗️ 架构 + 数据流 |
| 技术栈是否变化（依赖增删、模型切换、框架升级） | 🔧 技术栈 |
| 安装/运行步骤是否变化 | 🚀 快速开始 |
| 新增变更是否记录到变更表 | 📝 变更记录 |

## 🟡 建议规则

- 代码中的 docstring 和注释保持与实现同步
- 新增模块应在 `weather_agent/__init__.py` 的 docstring 中注册
- 日志格式变更需同步更新 README 中的日志格式示例

## 🟢 无需检查的情况

- 纯 bugfix（不改变行为/接口的修复）
- 注释/格式调整
- .gitignore 变更
